# FOC（磁场定向控制）项目进度文档

> 本项目用于秋招嵌入式/电机控制岗位。边学原理边写代码。
> 文档随项目推进实时更新。

---

## 📌 项目概述

**FOC（Field Oriented Control，磁场定向控制）**：一种无刷电机控制算法，通过坐标变换把三相交流电流转化为类似直流电机的励磁/转矩分量，实现对电机力矩的精确、线性控制。

**典型应用**：电动汽车、无人机、伺服电机、电动工具、机器人关节。

---

### 🎯 本项目目标

做一个 **单轴 FOC 云台稳定器**（秋招嵌入式岗项目）。FOC 核心算法手写（Clarke/Park/SVPWM/PID），不依赖 SimpleFOC 库，控制范围定为电压模式有感闭环。

### 🔧 硬件清单

| 部件 | 型号 | 作用 |
|------|------|------|
| MCU | **STM32F407VGT6** | 大脑，跑 FOC 算法（M4F@168MHz，硬件FPU） |
| 驱动板 | **SimpleFOC 板（MS8313≈DRV8313）** | 3 路 PWM 驱动三相 MOS，内置死区，**无相电流采样** |
| 电机 | **2804 云台电机**（M创动工坊）| **7 对极** / 额定 **12V** / KV~2000 / ~300g·cm |
| 角度传感器 | **AS5600 磁编码器**（I2C，12 位） | 测电机角度，有感闭环的"眼睛" |
| 姿态传感器 | **MPU6050** | 测云台姿态，做稳定控制 |

### 💻 开发环境

**VS Code + STM32CubeMX + CMake + arm-none-eabi-gcc** 工具链；**ST-Link V2** 烧录/调试。无示波器，用串口上位机（VOFA+）画波形 + Keil/VS Code 在线调试。

### 📌 引脚分配表（CubeMX 配置依据）

| 外设 | 引脚 | 说明 |
|------|------|------|
| TIM1_CH1 | PE9 | PWM → MS3131 IN_A（TIM1 完全重映射） |
| TIM1_CH2 | PE11 | PWM → MS3131 IN_B |
| TIM1_CH3 | PE13 | PWM → MS3131 IN_C |
| I2C1_SCL | PB6 | AS5600 + MPU6050 共用 |
| I2C1_SDA | PB7 | AS5600 + MPU6050 共用 |
| USART1_TX | PA9 | VOFA+ 调试串口（921600, 8N1） |
| USART1_RX | PA10 | VOFA+ 调试串口 |
| MS3131 EN | → 3V3 | 永久使能 |
| MS3131 nSLEEP (nSP) | → 3V3 | ⚠️必须接高，否则芯片休眠不工作 |
| MS3131 nFAULT (nFT) | 悬空 | 故障输出，可选接 GPIO 监测 |
| MS8313 VM | → **12V** | ⚠️电机电源≥8V（2804额定12V，MS8313欠压保护UVLO）|
| LED | 先用串口代替 | 板载 LED 脚待查，不阻塞进度 |

---

## 🗺️ 学习路线 & 进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| ① | 硬件 & 平台选型 | ✅ 完成（F407+MS8313+2804+AS5600 全到位）|
| ② | 电机原理 & 坐标变换（abc→αβ→dq） | ✅ 完成（反Park 已写）|
| ③ | 电流采样（ADC + 运放） | ➖ 不做（驱动板无电流采样，走电压模式）|
| ④ | SVPWM 空间矢量调制 | ✅ 完成（零序注入法，**电机已开环转动 🎉**）|
| ⑤ | 速度环 + 位置环 PID（无电流环）| ✅ 完成（**速度PI + 位置P 已通**，电机保位 + 手拨回弹 🎉）|
| ⑥ | 编码器反馈（AS5600 有感闭环）| ✅ 完成（对齐 + 闭环自同步，**电机闭环自转 🎉**）|
| ⑦ | MPU6050 姿态解算 + 云台联调 | ✅ 代码完成（**MPU6050驱动+互补滤波已通** 🎉 pitch稳定不抖；姿态环 attitude_loop 已接入串级控制链；⚠️**未做机械云台联调**，整机体感待机械件）|
| ⑧ | ~~进阶：无感 SMO / 高频注入~~（**本项目不纳入**，仅作了解） | ➖ 不做 |

---

## 🧩 技术决策记录（Decision Log）

> 每个关键选型都记录在这里，写明"选了什么 + 为什么"，方便复习回顾。

- **硬件路线**：✅ **真机调试**（有 STM32 + 电机驱动套件，可烧录验证）
- **调试约束**：⚠️ **无示波器** → 电流/角度/速度等数字量波形用 **串口上位机（VOFA+ 等）** 实时绘制；用 **Keil + ST-Link 在线调试** 看变量/寄存器；PWM/死区等模拟信号靠 **代码精细配置 + 成熟驱动电路** 保证。
- **MCU 选型**：✅ **STM32F407VGT6**（Cortex-M4F @168MHz，硬件 FPU；TIM1/TIM8 双高级定时器；ADC1/2/3 支持三重同步采样；192KB SRAM）
- **电机类型**：✅ 云台无刷电机 **2804**（GBM2804，低 KV，约 7 对极，低速大力矩，适合 FOC）
- **传感器方案**：✅ **AS5600 磁编码器**（I2C，12 位绝对角度 0~360°）→ **有感控制**
- **驱动方案**：✅ **SimpleFOC 驱动板，主芯 MS8313**（= DRV8313 国产替代，3 路 PWM 输入的三相半桥预驱，内置死区；简易板**无相电流采样**）
- **项目目标**：🎯 **秋招国企/央企**（嵌入式岗）—— 偏好真实、扎实、能独立讲清楚
- **项目定位**：⚖️ 真实、扎实、可独立复现；控制范围锁定电压模式有感闭环（无感观测器 / 电流闭环不在本阶段）
- **控制路线**：✅ **电压模式 FOC**（云台电机 + 驱动板无电流采样 → 天然适配）
- **核心算法**：✍️ **手写**（Clarke/Park/SVPWM/PID）—— 参数与行为完全可控，出了问题能从头查
- **项目特色**：✅ **单轴 FOC 云台稳定器**（+ MPU6050 姿态控制）
- **开发环境**：✅ **VS Code + STM32CubeMX + CMake + arm-none-eabi-gcc**，ST-Link V2 烧录/调试

---

## 📝 学习笔记 & 知识点

> 每个阶段学到的关键原理、公式、代码片段记录于此。

### 📓 开环 FOC 代码对照（参考工程 STM32F407-FOC-Open-loop）

店铺资料 `STM32F407-FOC-Open-loop-main.zip` 与本项目高度一致，逐行对照结论：

| 项 | 参考工程 ZH_FOC.c | 本项目 foc.c/h | 结论 |
|----|-------------------|---------------|------|
| PWM 引脚 | PE9/PE11/PE13 (TIM1_CH1/2/3) | 同 | ✅ 引脚选对 |
| ARR | 4200（中心对齐，20kHz）| 4200 | ✅ 一致 |
| 母线电压 | `voltage_power_supply = 12` | VDC 须由 5.0f 改 **12.0f** | ⚠️ 必改 |
| 反Park | Ud=0 简化：`Uα=−Uq·sinθ, Uβ=Uq·cosθ` | 完整式（带Vd），代入Vd=0 与参考等价 | ✅ 数学等价，更通用 |
| SVPWM | `Ux + VDC/2` 简单中心对齐 | 动态零序注入 `Vn=−(max+min)/2`（七段式等效）| ✅ 本项目更优，电压利用率 +15% |
| 开环 Uq | `VDC/3 = 4V` | `OPEN_LOOP_VQ=1.0f`（转不动调 2~3V）| ℹ️ 可调 |
| 极对数 | **11**（5008 类大电机）| 2804 = **7** | ⚠️ 别抄 11 |
| 开环调度 | 主循环 + HAL_GetTick 算 Ts | TIM6 中断固定周期 | ✅ 本项目更规范 |
| PWM 路数 | 6 路互补 + 死区（另一种驱动板）| 3 路（MS8313 内置死区）| ✅ 适配 MS8313，勿抄 PWMN |

**核心结论**：本项目 FOC 算法方向正确，SVPWM 写法比参考工程更标准。当前唯一阻塞 = **12V VM 电源未上**。

### 📓 AS5600 有感闭环调试记录

**对齐（foc_align_sensor）**：施加 `inv_park(0, ALIGN_UQ, 0)` 持续 700ms，把转子吸到 d 轴平衡点，读此时机械角 ×7 = `zero_electric_angle`。
- **7 对极 = 7 个机械平衡点**（每 360°/7≈51.4° 一个，电角度差 2π）；
- 复位后转子停位不同 → zero 落在 7 个聚集点之一，相邻差 **≈6.28（2π）**，电角度等价，闭环 mod 2π 后效果相同；
- `703.706` = I2C 失败特征值（0xFFFF×2π/4096×7），主循环用 `if(a<100)` 过滤。

**闭环自同步（foc_closedloop_step）**：电角度改用 AS5600 真实角 `elec = mech×7 − zero`，固定 Uq 驱动。
- 🔑 **关键坑：必须补 `+ PI/2`**！对齐把 d 轴吸到 90° 方向，若闭环不补 90°，磁场会压在 d 轴上（只有吸力、无推力）→ 现象：**电机不转，手拨一顿一顿**（位置锁定）。
- 补 `+ PI/2` 后磁场移到 q 轴（超前转子 d 轴 90°）→ 产生驱动力矩 → **电机闭环自转** ✅。
- 反转 → 改 `− PI/2`；仍顿挫 → AS5600 方向反，`mech×7` 取负。

**架构**：主循环读 AS5600 → 全局 `mech_angle`（带失败过滤）；TIM6 中断（1ms）取最新 `mech_angle` 算 FOC。I2C 阻塞 ~0.5ms 不放中断，避免拖慢控制周期。

### 📓 I2C 抗干扰排查（关键硬件坑）

**现象**：Uq=3V 时 `vel=1.0`（异常）+ 电机"飞快"但断电即停（无惯性）；Uq=1V 时 `vel=36.7`（正常）+ 断电能转一会（有惯性）。

**根因**：**I2C 被电机大电流噪声干扰**。Uq 大→电流大→PWM 噪声大→I2C 读取失败→`mech_angle` 不准→测速异常 + FOC 失步震荡（视觉"飞快"实为高频抖动，无整圈惯性）。Uq 小→电流小→I2C 正常。

**排查手法**：① 磁铁跟转子转→排除打滑；② **断 12V 看有无惯性**→区分"真转(有惯性) vs 失步抖动(无惯性)"；③ 降 Uq 验证→I2C 恢复。

**缓解**：`UQ_LIMIT=1.0V` 限幅控在小电流区，绕开干扰。**根治**：I2C 上拉电阻加强(4.7kΩ)+ 线远离电机线 + VM 滤波电容（后续 MPU6050 同总线，必须加固）。

### 📓 速度环 PI（电压模式控速）

**结构**：测速(差分+低通) → PI(`err=目标-vel_filt → uq_out`) → FOC。`uq_out` 替换固定 Uq，串在 TIM6 中断（测速→PI→驱动）。
- **P** 响应快但有稳态误差；**I** 消除稳态误差；**积分+Uq 双限幅**防饱和/失步。
- 调参：Kp=0.3, Ki=6.0 → vel 收敛 ~9.5（目标10），稳态差0.5 + 波动±0.5（电压模式 + 摩擦 + I2C噪声，可接受）。
- `VEL_TARGET` 带符号→控正反转；改值→控转速。

### 📓 角度环 P（位置控制，串级外环）

**结构**：位置环(外 P) → 速度环(内 PI) → FOC。位置环决定"去哪"（输出 `vel_target`），速度环决定"怎么平滑到那"。
- `err = angle_target - mech_angle`（带跨界最短角差处理）；`vel_target = ANGLE_KP × err`（限幅 `VEL_LIMIT`）。
- **串级思想**：外环慢、内环快；外环的输出 = 内环的目标。位置环只管位置误差→换成速度指令，剩下的稳速交给速度环。
- 🔑 **调参坑**：`ANGLE_KP`/`VEL_LIMIT` 太大（初版 2.0/10）→ 电机高速冲目标 → 惯性过冲 → 来回冲振荡 → 现象像"一直转停不住"。降到 `ANGLE_KP=0.5`、`VEL_LIMIT=3.0`（最大 0.5 圈/秒）→ 低速平滑逼近 → 能停、手拨松手回弹。**这是位置环最常踩的坑。**
- 验证标准：① 能停在 `angle_target`；② 手拨扰动松手能回原位。两关都过 = 位置环闭环成功。

---

### 📓 MPU6050 姿态解算（互补滤波）

**MPU6050 = 6 轴 IMU**：加速度计(静态准/动态脏) + 陀螺仪(动态准/积分漂移)，互补必须融合。I2C 地址 `0xD0`(0x68<<1，AD0 接 GND)，与 AS5600(`0x36`) 共用 I2C1，地址不冲突。
- 寄存器：PWR_MGMT_1(`0x6B` 写 0 唤醒) / 加速度(`0x3B`) / 陀螺仪(`0x43`)，各 6 字节高字节在前，`int16_t` 有符号。量程默认 ±2g(16384 LSB/g)、±250°/s(131 LSB/°/s)。

**互补滤波**：`pitch = 0.96×(pitch + gyro_y·dt) + 0.04×pitch_acc`
- `pitch_acc = atan2(−ax,az)·180/π`（加速度计，长期校正漂移）；`gyro_y = gy/131`（陀螺仪，短期抗振）
- α=0.96：陀螺仪当主力，加速度计少量校正漂移。🔑 `gyro_y` 正方向必须与 `pitch_acc` 增大方向一致，否则融合后 pitch 漂移。
- dt 用 `HAL_GetTick()` 实测（主循环节奏不固定），钳制 ≤0.05s 防异常。
- **不用卡尔曼**：单轴低速云台精度瓶颈在电压模式驱动 + I2C 干扰，不在算法；互补滤波一参数、计算量小，带宽满足云台需求。
- 验证：静止 pitch≈0 极平滑（电机在跑也不抖）→ 证明陀螺仪压住震动噪声，云台可用。

### 📓 姿态环 attitude_loop（云台控制外环）

**结构**：pitch 反馈（外 P）→ vel_target → 速度环（内 PI）→ FOC。用 pitch 替代 mech_angle 作外环反馈，即「云台水平稳定」。
- `err = −pitch · π/180`（pitch 单位度转弧度，负号 = pitch 偏正时反向校正）；`vel_target = ATTITUDE_KP × err`（限幅 VEL_LIMIT）。
- 🔑 **坑**：互补滤波那行 `pitch = ...` 必须写**全局 pitch**（foc.c 定义、foc.h extern），不能声明局部 `float pitch=0`（局部每轮重置 0 → 互补滤波积分丢失，pitch 退化成 pitch_acc 的 4%）。
- **架构位置**：`foc_attitude_loop` 与 `foc_angle_loop` 二选一挂 TIM6 中断；当前 callback 用 `foc_angle_loop`（位置环保位，电机安全），attitude_loop 待机械联调时切换。
- ⚠️ **未做机械联调**：缺机械云台结构，attitude_loop 为代码级实现，控制逻辑闭环但未整机体感测试。

### 📓 OpenOCD 一键烧录（替代调试器）

VSCode 调试器烧录又慢又易端口冲突（ST-LINK_gdbserver 端口被占），改用 OpenOCD 命令行：CMakeLists 加 `flash` 目标，`cmake --build build/Debug --target flash` 一条命令完成「编译+烧录+校验+复位+退出」，用完即退不占端口。
- ⚠️ build 目录是 `build/Debug`（STM32CubeIDE 扩展结构），不是 `build`。
- OpenOCD 配置：`interface/stlink.cfg` + `target/stm32f4x.cfg`，自动连 ST-LINK V2。

## 📁 项目文件结构

```
FOC/                       ← CubeMX 工程根目录
├── README.md              ← 项目门面（开源仓库主页）
├── LICENSE                ← MIT
├── .gitignore             ← Git 忽略规则
├── 项目进度.md            ← 项目主线文档（本文件）
├── 硬件接线.md            ← 完整接线表 + 避坑指南
├── CMakeLists.txt         ← CMake 构建（含 OpenOCD flash 目标）
├── Core/                  ← HAL 用户代码
│   ├── Inc/  (foc.h, as5600.h, mpu6050.h, main.h …)
│   └── Src/  (foc.c, as5600.c, mpu6050.c, main.c …)
└── Drivers/               ← HAL 库 + CMSIS
```
