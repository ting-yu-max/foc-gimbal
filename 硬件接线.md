# FOC 云台稳定器 —— 硬件接线说明

> 自接线方案（单独模块 + 杜邦线）。接线前请通读「⚠️ 注意事项」，避免炸管 / 不工作。

## 一、整体框图

```
   ┌─[电脑]──USB──┐                            ┌─[电机电源5~7V]
   │              │                            │
[ST-Link V2]  [USB转串口]                  [MS3131驱动板]──OUT_A/B/C──[2804电机]
   │ SWD          │ TX/RX                       ▲ IN_A/B/C, EN, nSP
   │              │                             │ 3V3, GND
   └──────────────┴─────────────────────────────┤
                       │                        │
                ┌──────────────────────┐        │
                │   STM32F407VGT6      │────────PE9/PE11/PE13 (PWM)
                │  PE9/11/13 = PWM     │────────PB6/PB7 (I2C) → [AS5600]
                │  PB6/7   = I2C       │────────PA9/PA10 (USART) → [串口]
                │  PA9/10  = USART     │────────3V3 → MS3131 EN/nSP、AS5600 VDD
                │  PA13/14 = SWD       │────────GND → 所有模块共地 ⚠️
                └──────────────────────┘
```

## 二、分组接线表（照着接）

### 1️⃣ ST-Link V2 → F407（烧录/调试，4 线 SWD）
| ST-Link V2 | F407 核心板 | 说明 |
|-----------|------------|------|
| SWDIO | SWDIO (PA13) | 数据 |
| SWCLK | SWCLK (PA14) | 时钟 |
| GND | GND | 共地 |
| 3.3V | 3V3 | 供电（若 F407 无独立电源则接；有则可不接）|

### 2️⃣ F407 → MS3131 驱动板（PWM + 使能，6 线）
| F407 | MS3131 | 说明 |
|------|--------|------|
| PE9  | IN_A | A 相 PWM |
| PE11 | IN_B | B 相 PWM |
| PE13 | IN_C | C 相 PWM |
| 3V3  | EN   | 使能（高有效）|
| 3V3  | **nSP (nSLEEP)** | ⚠️**必须接高，否则芯片休眠电机不转** |
| GND  | GND  | 共地 |
| —    | nFT (nFAULT) | 故障输出，先悬空不接 |

### 3️⃣ MS3131 → 电机 + 电机电源
| MS3131 | 接 | 说明 |
|--------|---|------|
| OUT_A | 电机 A 相 | 三相顺序接错只反转，不炸管，可后调 |
| OUT_B | 电机 B 相 | |
| OUT_C | 电机 C 相 | |
| VM    | 电机电源 12V（⚠️必须≥8V）| MS8313/DRV8313 低于8V触发欠压保护UVLO，芯片不工作 |
| GND   | 电机电源 −（共地）| |

### 4️⃣ F407 → AS5600 磁编码器（I2C，4 线）
| F407 | AS5600 | 说明 |
|------|--------|------|
| PB6 | SCL | I2C 时钟 |
| PB7 | SDA | I2C 数据 |
| 3V3 | VDD | 供电 |
| GND | GND | 共地 |
| — | DIR | 接 GND 定方向（模块一般已处理）|
| — | OUT | 不用（我们走 I2C 读数）|

### 5️⃣ F407 → USB 转串口（VOFA+ 调试，3 线）
| F407 | USB 串口模块 | 说明 |
|------|-------------|------|
| PA9 (TX)  | RX | ⚠️**交叉接**（TX→RX）|
| PA10 (RX) | TX | ⚠️**交叉接**（RX→TX）|
| GND | GND | 共地 |

### 6️⃣ MPU6050：暂不接
云台稳定阶段才用，届时也挂 **PB6/PB7 同一条 I2C**（地址 0x68，与 AS5600 的 0x36 不冲突）。

## 三、电源方案

- **逻辑电 3.3V**：F407、MS3131 逻辑、AS5600 —— 由 ST-Link 的 3.3V 或 F407 板载 3V3 口供电。
- **电机电源 VM**：**12V（必须 ≥8V）** —— MS8313/DRV8313 最低工作电压 8V，低于 8V 触发欠压锁定（UVLO），芯片完全不工作（电机无反应的头号原因）。母线 12V ≠ 电机承受电压，FOC 的 Vq 才决定电机实际电压，安全。
- ⚠️ **所有模块的 GND 必须全部连通（共地）**：F407、MS3131、AS5600、串口模块、ST-Link、电机电源的 GND 全接一起。

## 四、⚠️ 注意事项（避坑）

1. **共地！共地！共地！** 任何两个模块不共地，信号就传不动（I2C 不通、PWM 无效、串口乱码）。
2. **nSP（nSLEEP）必须接 3.3V** —— 忘接是"电机不转"头号原因。
3. **串口 TX/RX 交叉接** —— F407 的 TX 接模块的 RX，别接错。
4. **AS5600 磁铁安装**：磁铁贴在电机轴尾部，AS5600 芯片圆心对准磁铁中心，间距 **1~3mm**，两者平行。若 2804 是"带编码器"版本则已装好。
5. **调试初期先别给 VM 上电**：先只接好信号线，让 F407 跑起来、PWM 有输出、I2C 能读到 AS5600，确认软件 OK 后，**再给 MS3131 的 VM 上电让电机转**（防止程序 bug 导致异常驱动）。
6. **I2C 不通？** AS5600 模块通常板载上拉电阻；若不通，检查 SDA/SCL 有无 4.7kΩ 上拉到 3.3V。
7. **电机三相接错**：只会反转/抖动，不会炸管，代码里 swap 两相即可改向。
